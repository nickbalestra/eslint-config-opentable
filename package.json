{
  "name": "ot-script-loader",
  "version": "0.0.3",
  "description": "",
  "main": "lib/loader.js",
  "scripts": {
    "test": "npm run lint",
    "lint": "eslint src/**",
    "prebuild": "npm run lint",
    "dist": "rollup -c",
    "build": "babel src --out-dir lib",
    "prepublish": "npm run build && npm run dist && NODE_ENV=production npm run dist"
  },
  "author": "",
  "license": "ISC",
  "files": [
    "lib",
    "dist"
  ],
  "devDependencies": {
    "babel-cli": "^6.11.4",
    "babel-plugin-external-helpers": "^6.8.0",
    "babel-preset-es2015": "^6.13.2",
    "eslint": "^3.3.0",
    "rollup": "^0.34.8",
    "rollup-plugin-babel": "^2.6.1",
    "rollup-plugin-commonjs": "^3.3.1",
    "rollup-plugin-node-resolve": "^2.0.0",
    "rollup-plugin-uglify": "^1.0.1"
  },
  "dependencies": {
    "little-loader": "0.1.1"
  }
}
