{
  "name": "ot-script-loader",
  "version": "0.1.3",
  "description": "",
  "main": "lib/loader.js",
  "scripts": {
    "test": "npm run lint",
    "lint": "eslint src/**",
    "prebuild": "npm run lint",
    "dist": "rollup -c",
    "build": "babel src --out-dir lib",
    "prepublish": "NODE_ENV=production npm run build && npm run dist && NODE_ENV=production npm run dist"
  },
  "author": "",
  "license": "ISC",
  "files": [
    "lib",
    "dist"
  ],
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-plugin-external-helpers": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "eslint": "^3.13.0",
    "rollup": "^0.41.0",
    "rollup-plugin-babel": "^2.7.1",
    "rollup-plugin-commonjs": "^7.0.0",
    "rollup-plugin-node-resolve": "^2.0.0",
    "rollup-plugin-replace": "^1.1.1",
    "rollup-plugin-uglify": "^1.0.1"
  },
  "dependencies": {
    "little-loader": "0.2.0"
  }
}
